name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g. v0.1.0)'
        required: true
        type: string
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: |
          npm install --ignore-scripts --registry https://registry.npmjs.org/
          npm install --prefix backend --ignore-scripts --registry https://registry.npmjs.org/
          npm install --prefix ui --ignore-scripts --registry https://registry.npmjs.org/

      - name: Build
        run: npm run build

      - name: Package distributable
        run: |
          sudo apt-get install -y zip
          chmod +x scripts/package-release.sh
          ./scripts/package-release.sh "$VERSION"
        env:
          VERSION: ${{ github.event.inputs.version || github.ref_name }}

      - name: Upload Release Asset
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.version || github.ref_name }}
          files: |
            my-openclaw-*.tar.gz
            my-openclaw-*.zip
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
